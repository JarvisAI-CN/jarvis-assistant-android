<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 基础权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 麦克风权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- 文件管理权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 相机权限 (用于视频通话) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 定位权限 (用于位置服务) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 传感器权限 -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- 设备信息 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- 后台运行权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- 多设备连接 -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- 硬件加速 -->
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 应用配置 -->
    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="false" />

    <application
        android:name=".core.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoipAssistant"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 主 Activity -->
            <activity
                android:name=".presentation.MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.VoipAssistant"
                android:windowSoftInputMode="adjustResize">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
                <intent-filter>
                    <action android:name="android.intent.action.VIEW" />
                    <category android:name="android.intent.category.DEFAULT" />
                    <category android:name="android.intent.category.BROWSABLE" />
                    <data
                        android:host="assistant"
                        android:scheme="voip" />
                </intent-filter>
            </activity>

            <!-- 通话 Activity -->
            <activity
                android:name=".features.call.ui.CallActivity"
                android:exported="true"
                android:theme="@style/Theme.VoipAssistant"
                android:windowSoftInputMode="stateHidden">
                <intent-filter>
                    <action android:name="com.assistant.voip.CALL" />
                    <category android:name="android.intent.category.DEFAULT" />
                </intent-filter>
            </activity>

            <!-- 文件传输 Activity -->
            <activity
                android:name=".features.file.ui.FileTransmissionActivity"
                android:exported="false"
                android:theme="@style/Theme.VoipAssistant"
                android:windowSoftInputMode="stateHidden">
            </activity>

            <!-- 任务管理 Activity -->
            <activity
                android:name=".features.task.ui.TaskManagementActivity"
                android:exported="false"
                android:theme="@style/Theme.VoipAssistant"
                android:windowSoftInputMode="stateHidden">
            </activity>

            <!-- 服务 -->
            <service
                android:name=".core.services.VoipService"
                android:exported="true"
                android:foregroundServiceType="mediaProjection|microphone" />

            <service
                android:name=".core.services.SpeechRecognitionService"
                android:exported="false" />

            <service
                android:name=".core.services.TaskExecutionService"
                android:exported="false" />

            <!-- 接收器 -->
            <receiver
                android:name=".core.receivers.CallReceiver"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.PHONE_STATE" />
                    <action android:name="com.assistant.voip.INCOMING_CALL" />
                </intent-filter>
            </receiver>

            <receiver
                android:name=".core.receivers.NetworkReceiver"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                    <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                </intent-filter>
            </receiver>

            <!-- 通知渠道 -->
            <meta-data
                android:name="android.app.default_notification_channel_id"
                android:value="@string/notification_channel_call" />

            <!-- WebRTC -->
            <meta-data
                android:name="org.webrtc.sdk.library.version"
                android:value="1.0.32006" />

            <!-- 应用程序配置 -->
            <meta-data
                android:name="com.assistant.voip.version"
                android:value="1.0.0" />

            <!-- 百度云API配置 -->
            <meta-data
                android:name="com.baidu.speech.app_id"
                android:value="YOUR_APP_ID" />
            <meta-data
                android:name="com.baidu.speech.api_key"
                android:value="YOUR_API_KEY" />
            <meta-data
                android:name="com.baidu.speech.secret_key"
                android:value="YOUR_SECRET_KEY" />

    </application>
</manifest>
